// const frutas = ['Pera', 'Maçã', 'Uva'];

// for (let i in frutas) {
//     console.log(frutas[i]);
// }

const pessoa = {
    nome: 'Emerson',
    sobrenome: 'Santos',
    idade: 30,
};

for (let chave in pessoa) {
    console.log(chave, pessoa[chave]);
}

